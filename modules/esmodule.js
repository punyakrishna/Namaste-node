// by default it will be common js pattern if we wish to use the ESmodule then we have to set the type to module in package json
// this is async way
// {
//     "type":"module"
// }

//Runs in strict mode but common js patterns runs in non-strict mode

// z = "Punya"; //gives you a error -- z is not defined and in common js patterns it will run

// export function sumInEsModule(a, b) {
//   console.log(a + b);
// }
